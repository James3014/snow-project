# SnowTrace 部署環境變數
# ⚠️ 需要手動在 Zeabur/Vercel 設定

# JWT 配置 (所有服務都需要)
JWT_SECRET_KEY=jmNrssrb29z4GzzAo1tojsMQFXGWpdKjIL8jiS7hAZSQ8MHffq8Q1YbgpjvWC_AJ_7G1cpC1EeCt5xWdKV94ww
JWT_ALGORITHM=HS256
JWT_AUDIENCE=user_core
JWT_ISSUER=SnowTrace

# 服務間通信 URLs (部署後手動更新)
USER_CORE_API_URL=https://user-core.zeabur.app
RESORT_API_BASE_URL=https://resort-api.zeabur.app  # 👈 部署 resort-api 後更新
SNOWBUDDY_API_URL=https://snowbuddy.zeabur.app     # 👈 部署 snowbuddy 後更新

# 資料庫 (Zeabur 提供，需要手動複製)
DATABASE_URL=<從_zeabur_dashboard_複製>
REDIS_URL=<從_zeabur_dashboard_複製>

# API Keys (手動生成)
USER_CORE_API_KEY=<手動生成隨機字串>
RESORT_API_KEY=<手動生成隨機字串>
SNOWBUDDY_API_KEY=<手動生成隨機字串>

# 手動部署步驟：
# 1. 在 Zeabur 建立新服務
# 2. 連接 GitHub repo
# 3. 在環境變數頁面貼上上述變數
# 4. 部署完成後更新其他服務的 URL 變數
